{
    model {
        skip_typecheck = true
    }
    cloud {
        platform = aws
        aws {
            region = eu-central-1
            # These will be picked up from ~/.aws/credentials or ENV
            # secret_key = 123
            # access_key = 123

            worker_count = 1
            timeout = 1000
        }
    }
}